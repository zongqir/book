# todo_plan｜AI 一次性完善执行计划

## 目标

- AI 的任务不是补默认核心套件，而是一次性完成第二层收口：
  - 补 `01_核心原则.md`
  - 强化 `03_最有价值的案例.md`
  - 强化 `05_方法论总结.md` / `06_适用边界与失效条件.md` 的论述依据
- 不在动作清单里的书，不额外改写。

## 输入文件

- 总说明：[todo](C:/Users/Administrator/code/mine/life/book/todo)
- 逐书动作清单：[todo2_books.json](C:/Users/Administrator/code/mine/life/book/scripts/todo2_books.json)

## 总规则

- 只处理 `scripts/todo2_books.json` 里 `actions` 明确列出的动作。
- `actions` 里没有 `补01_核心原则`，就不要新建 `01_核心原则.md`。
- `actions` 里没有 `可补更强案例`，就不要为了“更完整”硬加案例段。
- `actions` 里没有 `可补更强论述依据`，就不要为了“更扎实”硬补论证。
- 允许 AI 在一次任务里同时处理 `补01`、`补案例`、`补论述依据` 三类动作，但每本书只做 `actions` 指定的那几项。
- 必须直接回写仓库文件，不只给建议，不拆成周计划。
- 批量执行时优先按分类连续处理，减少风格漂移，但不能因此把任务拆成多周。

## 停写条件

- 一本书如果 `actions` 只有 `无新增动作`，跳过。
- 一本书如果已经有 `01_核心原则.md`，这次不再重写，除非用户明确要求。
- 案例强化只在原页已有案例框架但明显偏空 无法完整体现这本书关键信息、偏泛时才做；原页已经够用就停。
- 论述依据强化按照Skills论述来，不扩成长篇书评。

## 一次性执行顺序

### Step 1｜先扫动作清单并分组三类任务

- 把 `scripts/todo2_books.json` 中全部书按 `actions` 分成三组：
  - `补01_核心原则`
  - `可补更强案例`
  - `可补更强论述依据`
- 同一本书可能同时落入多组，允许一次做完，但动作边界不能串。
- 先处理结构性最稳定的缺口，再处理增强项：
  1. `补01_核心原则`
  2. `可补更强案例`
  3. `可补更强论述依据`

### Step 2｜一次性补完全部 `01_核心原则`

- 目标：把 `81` 本缺 `01` 的书一次性补齐。
- 优先分类：
  - `12_经济分析`
  - `21_哲学思想`
  - `23_心理机制`
  - `14_国际政治`
  - `13_国家治理`
  - `15_中国历史`
  - `22_心智成长`
  - `20_学习方法`
- 每本书只新建或补写 [01_核心原则.md](C:/Users/Administrator/code/mine/life/book/site/content/library)，不顺手改写别的槽位。

### Step 3｜一次性强化全部需要补案例的书

- 目标：把 `132` 本需要更强案例支撑的书一次性推进到更能调用的状态。
- 优先分类：
  - `03_沟通表达`
  - `17_职场协作`
  - `24_自我经营`
  - `19_商业战略`
  - `25_写作文学`
  - `18_管理领导`
- 默认只改 [03_最有价值的案例.md](C:/Users/Administrator/code/mine/life/book/site/content/library)；除非现有结构明确要求，否则不扩写到其他页。

### Step 4｜一次性强化全部需要补论述依据的书

- 目标：把 `94` 本判断型、理论型、历史型书一次性推进到论证更稳的状态。
- 优先分类：
  - `12_经济分析`
  - `21_哲学思想`
  - `23_心理机制`
  - `14_国际政治`
  - `15_中国历史`
  - `13_国家治理`
  - `11_金融体系`
- 默认优先补强 [05_方法论总结.md](C:/Users/Administrator/code/mine/life/book/site/content/library) / [06_适用边界与失效条件.md](C:/Users/Administrator/code/mine/life/book/site/content/library) 中论证偏空、依据偏弱的部分，不扩成长篇书评。

### Step 5｜统一做一次总检查

- 检查所有改动是否只落在 `actions` 指定的书和槽位。
- 检查 frontmatter 是否完整。
- 检查没有把 `04_行动指南.md` 和 `05_方法论总结.md` 写重复。
- 检查没有把补案例写成观点摘要，也没有把补论述依据写成背景堆砌。

## AI 单次任务模板

给 AI 的任务，统一按这个模板发，不拆轮次：

```md
任务目标：依据 [todo](C:/Users/Administrator/code/mine/life/book/todo) 和 `scripts/todo2_books.json`，一次性完善全部已分析书目的第二层缺口。

必须完成：
- 对所有 `actions` 含 `补01_核心原则` 的书，新建或补齐 `01_核心原则.md`
- 对所有 `actions` 含 `可补更强案例` 的书，补强 `03_最有价值的案例.md`
- 对所有 `actions` 含 `可补更强论述依据` 的书，补强 `05_方法论总结.md` 或 `06_适用边界与失效条件.md`
- 直接回写仓库文件

不要做：
- 不处理 `actions` 只有 `无新增动作` 的书
- 不顺手重写整本书
- 不默认接 NotebookLM
- 不把案例强化写成摘要
- 不把论述依据强化写成空泛背景

完成标记：
- 每本书都只做 `actions` 指定动作
- 每个新增或修改文件 frontmatter 完整
- 最终能汇总：补了多少本 `01`、强化了多少本案例、强化了多少本论述依据
```

## 交付要求

- 这是一次性执行计划，不是周计划，不是分三周推进。
- AI 应在同一任务中完成扫描、改写、回写和自检。
- 如果中途发现个别书信息不足，只跳过该书并记录原因，不影响其余书一次性完成。

## 不要做的事

- 不把 `todo` 当成真实 backlog 继续追书目数量
- 不因为“还有空”就给所有书都补 `01`
- 不把案例强化写成观点摘要
- 不把论述依据强化写成大段空泛背景介绍
- 不在没有动作信号的书上继续消耗时间
